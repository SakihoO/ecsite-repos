import styles from './Footer.module.scss';

const Header = () => {
    return (
        <div className={styles.footer}>
            <p>Copyright ECsite Repos. All rights reserved.</p>
        </div>
    );
};

export default Header;